// codeforces 1715d

#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int n{}, q{};
    cin >> n >> q;
    if (n == 1 and q == 0) {
        cout << 0 << endl;
        return 0;
    }
    while (q--) {
        int i{}, j{}, x{};
        cin >> i >> j >> x;

    }
    return 0;
}
