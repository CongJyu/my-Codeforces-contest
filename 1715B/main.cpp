// codeforces 1715b

#include <iostream>

using namespace std;

void solution() {

    long long n{}, k{}, b{}, s{};
    cin >> n >> k >> b >> s;

}

int main() {
    ios::sync_with_stdio(false);
    int t{};
    cin >> t;
    while (t--) {
        solution();
    }
    return 0;
}
