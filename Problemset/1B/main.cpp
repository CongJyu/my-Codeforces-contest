// cf 1b

#include <iostream>
#include <string>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int n = 0;
    cin >> n;
    while (n--) {
        string in;
        cin >> in;
        
    }
    return 0;
}
