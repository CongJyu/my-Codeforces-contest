// codeforces 1607a

#include <iostream>
#include <string>
#include <cmath>

using namespace std;

void solution() {
    // remain to be completed
}

int main() {
    ios::sync_with_stdio(false);
    int t{};
    cin >> t;
    while (t--) {
        solution();
    }
    return 0;
}
