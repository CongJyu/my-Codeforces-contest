// codeforces 1728d

#include <iostream>
#include <string>
#include <stack>

using namespace std;

void solution() {
    string s;
    cin >> s;
    stack<char> a;
    stack<char> b;
    int left{}, right{(int) s.length() - 1};
}

int main() {
    ios::sync_with_stdio(false);
    int t{};
    cin >> t;
    while (t--) {
        solution();
    }
    return 0;
}
