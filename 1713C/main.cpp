// codeforces 1713c
// no idea

#include <iostream>
#include <cmath>
#include <vector>

using namespace std;

bool is_sqrt(int x) {
    if (floor(sqrt(x) + 0.5) == sqrt(x)) {
        return true;
    } else {
        return false;
    }
}

void solution() {
    int n{};
    cin >> n;
}

int main() {
    ios::sync_with_stdio(false);
    int t{};
    cin >> t;
    while (t--) {
        solution();
    }
    return 0;
}
