// codeforces 1665a

#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int t{};
    cin >> t;
    while (t--) {
        int n{};
        cin >> n;
        cout << n - 3 << " " << 1 << " " << 1 << " " << 1 << endl;
    }
    return 0;
}
