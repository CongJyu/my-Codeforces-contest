// codeforces 1716d

#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int n{}, k{};
    cin >> n >> k;
    for (int i{1}; i <= n; ++i) {
        // reach i
        int move{};

    }
    return 0;
}
