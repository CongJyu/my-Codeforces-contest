// codeforces 1770b

#include <iostream>

using namespace std;

void solution() {
    long long n{}, k{};
    cin >> n >> k;
}

int main() {
    ios::sync_with_stdio(false);
    int t{};
    cin >> t;
    while (t--) {
        solution();
    }
    return 0;
}
